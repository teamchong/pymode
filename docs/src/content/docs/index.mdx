---
title: pymode
description: Upstream CPython 3.13 on Cloudflare Workers, compiled to WebAssembly via zig cc.
template: splash
hero:
  tagline: Upstream CPython 3.13 on Cloudflare Workers. Write Python handlers with access to KV, R2, D1, and TCP — compiled to wasm via zig cc, deployed as a single AOT-built worker per app.
  actions:
    - text: Get Started
      link: /getting-started/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/teamchong/pymode
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

## About this project

This is a personal experiment in running upstream CPython 3.13 on Cloudflare Workers via `zig cc` → `wasm32-wasi`. Cloudflare's official Python Workers ship Pyodide and are the supported path; PyMode is not a replacement for them.

<CardGrid>
  <Card title="Upstream CPython 3.13" icon="seti:python">
    Vanilla CPython compiled with `zig cc` to `wasm32-wasi`. No Pyodide-specific patches; semantics match what `python3.13` does on a normal machine. [See bundled modules](/limitations/).
  </Card>
  <Card title="AOT host imports" icon="rocket">
    `pymode deploy` patches host import sites (KV, R2, D1, fetch) into direct JS stubs in the wasm. Skips the JsProxy/PyProxy translation Pyodide pays per call.
  </Card>
  <Card title="Compile any C extension" icon="puzzle">
    Pyodide ships a curated set of compiled wheels (numpy, pandas, Pillow…). PyMode lets you build arbitrary C extensions via `zig cc` to wasm32-wasi when you need a package Pyodide doesn't carry.
  </Card>
  <Card title="Wizer snapshot at deploy" icon="star">
    The build pre-imports stdlib + your project's modules into a memory snapshot baked into the wasm. Worker Startup Time on CF (the cost of bringing an isolate up) is typically 40–60 ms.
  </Card>
</CardGrid>

## How It Works

```
Request → Worker (stateless) → PythonDO (Durable Object)
                                    ↓
                            python.wasm (_start)
                                    ↓
                            on_fetch(request, env)
                                    ↓
                            KV / R2 / D1 / TCP
                                    ↓
                            Response → Client
```

Each request gets its own sandboxed Python runtime inside a Durable Object. The WASM stack is instrumented with Asyncify so host imports (HTTP fetch, KV reads, TCP recv) suspend and resume transparently.

<CardGrid>
  <Card title="HTTP Fetch" icon="external">
    `fetch("https://api.example.com")` calls the host directly — no network stack in WASM.
  </Card>
  <Card title="Parallel Execution" icon="random">
    `spawn()` launches child Durable Objects with separate 30s CPU budgets and 128MB memory.
  </Card>
  <Card title="Package Ecosystem" icon="setting">
    Pure-Python packages work out of the box. C/Rust extensions (numpy, pydantic_core) compile to WASM variants. AI frameworks (langchain, langgraph, instructor) tested and working.
  </Card>
  <Card title="Multi-File Projects" icon="document">
    Standard Python imports work. Organize your project with packages, modules, and `pyproject.toml`.
  </Card>
</CardGrid>
